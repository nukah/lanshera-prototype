<%= link_to t('home'), :root %> | 
<% if user_signed_in? %>
	<%= link_to current_user.email, manage_accounts_path %> - <%= link_to t('logout'), destroy_user_session_path, :method => :delete %>
<% else %>
	<%= link_to t('account'), new_user_session_path %>
<% end %>