<div class="auth_header">
	<%= content_tag(:h3, t('login')) %>
	<ul>
		<li><%= link_to "Зарегистрироваться",'#', :id => 'register_link' %></li>
		<li>Восстановить пароль</li>
	</ul>
</div>
<div class="auth_content">
	<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
	<fieldset>
	<dl>
		<dt><%= f.label :email %></dt>
		<dd><%= f.email_field :email %></dd>
	
		<dt><%= f.label :password %></dt>
		<dd><%= f.password_field :password %></dd>
	</dl>
	</fieldset>
	<div class="submit">
		<%= f.submit "Войти" %>
	</div>
	<div class="remember">
		<%= f.check_box :remember_me %> <%= f.label :remember_me, 'запомнить' %>
	</div>
	<% end %>
</div>