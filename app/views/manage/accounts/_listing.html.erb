<%= listing_counter + 1 %> : <%= listing.login %><BR>
<% if false %>
<%= link_to t('manage.accounts.edit_link'), edit_manage_account_path(listing.id) %> | 
<%= link_to t('manage.accounts.delete_link'), manage_account_path(listing.id), :method => :delete %><br>
<% end %>